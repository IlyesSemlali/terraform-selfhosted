cluster:
  name: ${cluster_name}
nodeinit:
  enabled: true
  reconfigureKubelet: "true"
  removeCbrBridge: "true"
cni:
  binPath: "/home/kubernetes/bin"
gke:
  enabled: true
ipam:
  mode: "kubernetes"
ipv4NativeRoutingCIDR: ${native_routing_cidr}
